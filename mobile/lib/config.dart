class AppConfig {
  // Production Render URL (used by default)
  // For local dev, use the Settings icon on login screen
  
  // Production Render URL
  static const String apiUrl = "https://finsight-1-a1ov.onrender.com/api";
}
